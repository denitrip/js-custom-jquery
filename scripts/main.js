//simply mount list inside the element with a "container" class;
////////////////////////////////
///	USAGE:
///	$.list(DOM, orientation);
////////////////////////////////
//orientation: true - vertical, false - horizontal. Orientation = false by default;

$.list($('.container'), false);
